- label: "build"
  command:
    - make clean
    - make prepare
    - make docs
  env:
    PATH:"$PATH:$(shell hab pkg path core/python2)/bin"
  plugins:
    docker#v1.1.1:
      image: "chefes/buildkite"
